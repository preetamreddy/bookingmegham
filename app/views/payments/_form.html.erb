<%= form_for(@payment) do |f| %>
  <% if @payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% @payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
   	<%= f.label :trip_id %><br />
		<% if @payment.trip_id %>
    	<%= f.collection_select(:trip_id, 
						Trip.scoped_by_account_id(current_user.account_id).
						find_all_by_id(@payment.trip_id), :id, :name) %>
		<% end %>
  </div>
  <div class="field">
    <%= f.label :payee_name %><br />
    <%= f.text_field :payee_name %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="field">
    <%= f.label :date_received %><br />
    <%= f.date_select :date_received %>
  </div>
  <div class="field">
    <%= f.label :payment_mode %><br />
		<%= f.select :payment_mode, Payment::PAYMENT_MODE, prompt: 'Select' %>
  </div>
  <div class="field">
    <%= f.label :details %><br />
    <%= f.text_field :details %>
  </div>
  <div class="action_container">
    <%= f.submit %>
  </div>
<% end %>
