<% if session[:guest_id] %>
	<h1>New Trip for Guest '<%= Guest.find(session[:guest_id]).name %>'</h1>
<% else %>
	<h1>New Trip</h1>
<% end %>

<%= render 'form' %>

<%= link_to 'Cancel', trips_path(:guest_id => session[:guest_id]), :class => "cancel_link" %>
